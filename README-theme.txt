Follow instructions here: http://bootswatch.com/help/

Mind that:
* we want the 2.3.2 version (git tag)
* we also need to bower install
* built file is build/readable/bootstrap.min.css 

All this should be fairly automated with setup.sh and build.sh (usage: build.sh cerulean)
